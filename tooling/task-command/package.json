{
  "name": "task-command",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx tsc -p tsconfig.json --noEmit",
    "test": "npx vitest run",
    "test:watch": "npx vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "vitest": "^4.0.2"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "dotenv": "^17.3.1"
  }
}
