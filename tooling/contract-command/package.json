{
  "name": "contract-command",
  "version": "1.0.0",
  "description": "Contract management TNDS Command Module",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "tsc --noEmit",
    "type-check": "tsc --noEmit --skipLibCheck"
  },
  "dependencies": {
    "@vercel/postgres": "^0.8.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/uuid": "^9.0.7",
    "typescript": "^5.6.3",
    "vitest": "^1.1.0"
  }
}
