{
  "name": "command-center",
  "version": "1.0.0",
  "description": "TNDS Command Center - Module registry, discovery, and routing service",
  "type": "module",
  "main": "dist/src/tools.js",
  "types": "dist/src/tools.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  }
}
