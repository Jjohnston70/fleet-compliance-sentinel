# COMMANDS.md — {{PROJECT_NAME}}

**Last Updated:** {{DATE}}
**Rule:** Run commands from the project root unless noted otherwise.

---

## Frontend

```bash
{{FRONTEND_DEV_COMMAND}}

{{FRONTEND_BUILD_COMMAND}}

{{FRONTEND_TEST_COMMAND}}

{{FRONTEND_LINT_COMMAND}}

{{FRONTEND_DEPLOY_COMMAND}}
```

---

## Backend

```bash
{{BACKEND_DEV_COMMAND}}

{{BACKEND_INSTALL_COMMAND}}

{{BACKEND_TEST_COMMAND}}

{{BACKEND_DEPLOY_COMMAND}}
```

---

## Database

```bash
{{DB_GENERATE_COMMAND}}

{{DB_MIGRATE_COMMAND}}

{{DB_STUDIO_COMMAND}}

{{DB_CONNECT_COMMAND}}
```

---

## Testing

```bash
{{TEST_ALL_COMMAND}}

{{TEST_WATCH_COMMAND}}

{{TEST_SINGLE_COMMAND}}
```

---

## Deployment

```bash
{{DEPLOY_PRODUCTION_COMMAND}}

{{DEPLOY_PREVIEW_COMMAND}}

{{DEPLOY_LOGS_COMMAND}}
```

---

## Git Workflow

```bash
# Create feature branch
git checkout -b feature/description-of-work

# Commit format
git commit -m "feat: description"
git commit -m "fix: description"
git commit -m "chore: description"

# Push and open PR — do not merge without approval
git push origin feature/description-of-work

# NEVER do these
{{GIT_FORBIDDEN_COMMANDS}}
```

---

## Compliance Evidence

```bash
# Capture audit log export (run weekly)
{{COMPLIANCE_EVIDENCE_COMMANDS}}
```

---

## Useful Shortcuts

```bash
# Check what's running on a port
lsof -i :{{PRIMARY_PORT}}
lsof -i :{{SECONDARY_PORT}}

# Kill a port
kill -9 $(lsof -t -i:{{PRIMARY_PORT}})

# Check environment variables are loaded
{{ENV_CHECK_COMMANDS}}
```

{{ADDITIONAL_COMMAND_SECTIONS}}
