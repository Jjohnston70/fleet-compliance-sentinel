# TODO - 2025-02-27

## Penny Markdown Rendering (Do Later)

- Replace plain text rendering in `src/app/penny/PennyChat.tsx` with a markdown renderer.
- Add `react-markdown` and `remark-gfm` for headings, lists, tables, and code blocks.
- Add `rehype-sanitize` for safe markdown HTML rendering.
- Add chat markdown styles in `src/styles/globals.css` (headings, lists, tables, quotes, code).
- Remove or adjust `renderTextWithLinks()` to avoid double-link processing.
- Test desktop/mobile rendering and deploy.
